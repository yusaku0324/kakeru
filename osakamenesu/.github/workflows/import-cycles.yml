name: Import Cycles

on:
  push:
  pull_request:

jobs:
  check-cycles:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Detect service-layer import cycles
        run: python tools/check_cycles.py
