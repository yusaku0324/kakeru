{
  "name": "osakamenesu",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "postinstall": "pnpm --dir apps/web install",
    "dev:api": "cd services/api && MEILI_HOST=http://127.0.0.1:7700 doppler run --project osakamenesu --config dev_web -- uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-exclude 'node_modules|.next|dist|venv|.git'",
    "dev:web": "doppler run --project osakamenesu --config dev_web -- pnpm --dir apps/web dev",
    "dev": "run-p dev:api dev:web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "packageManager": "pnpm@10.20.0"
}
