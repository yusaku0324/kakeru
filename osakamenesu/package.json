{
  "name": "osakamenesu",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "postinstall": "pnpm --dir apps/web install",
    "dev:api": "cd services/api && MEILI_HOST=http://127.0.0.1:7700 doppler run --project osakamenesu --config dev_web -- uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-exclude 'node_modules|.next|dist|venv|.git'",
    "dev:web": "doppler run --project osakamenesu --config dev_web -- pnpm --dir apps/web dev",
    "dev": "run-p dev:api dev:web",
    "lint": "pnpm --dir apps/web run lint",
    "ci:local": "doppler run --project osakamenesu --config stg_ci_local -- bash -c 'docker compose -f docker-compose.admin-e2e.yml -f docker-compose.admin-e2e.override.yml up -d db meili redis api web && docker compose -f docker-compose.admin-e2e.yml -f docker-compose.admin-e2e.override.yml up --no-deps --abort-on-container-exit --exit-code-from e2e --no-build e2e'"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "packageManager": "pnpm@10.20.0"
}
